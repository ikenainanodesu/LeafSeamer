import NodeCG from "nodecg/types";

export class DataFetcher {
  private nodecg: NodeCG.ServerAPI;

  constructor(nodecg: NodeCG.ServerAPI) {
    this.nodecg = nodecg;
    // Placeholder for future data fetching logic (e.g., from Google Sheets)
  }
}
