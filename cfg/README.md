# Configuration Files

Copy the example files in this directory before running LeafSeamer:

- `nodecg.json.example` -> `nodecg.json`
- `data-sync-service.json.example` -> `data-sync-service.json` when Google Sheets sync is needed

Real config files may contain local network addresses or credentials and are intentionally ignored by Git.
