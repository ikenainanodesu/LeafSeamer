{
  "postgresql": {
    "connectionStringEnv": "LEAFSEAMER_SCHEDULE_POSTGRES_URL",
    "query": "SELECT external_id, time, title, description, state, active, planned_at, revision FROM playlist ORDER BY planned_at",
    "sourceId": "postgresql",
    "pollIntervalMs": 60000,
    "statementTimeoutMs": 10000
  }
}
